import { useMemo } from "react";
const money = (v) => Number(v || 0).toLocaleString("pt-BR", { style:"currency", currency:"BRL" });
export default function InfinitePay({ data, onChange, currentUser }) {
  const tx = data.infinitePayTransacoes || [];
  const totals = useMemo(()=>tx.reduce((a,t)=>{const bruto=Number(t.valorBruto||0), taxa=Number(t.taxa||0), liquido=Number(t.valorLiquido ?? bruto-taxa);a.bruto+=bruto;a.taxa+=taxa;a.liquido+=liquido;if(t.status==="LIQUIDADO")a.saldo+=liquido;else a.receber+=liquido;return a;},{bruto:0,taxa:0,liquido:0,saldo:0,receber:0}),[tx]);
  const sync = () => onChange(d=>({...d,infinitePaySincronizacao:{status:"AGUARDANDO CONECTOR HOMOLOGADO",solicitadaEm:new Date().toISOString(),solicitadaPor:currentUser?.nome}}));
  return <section className="card infinitePanel"><div className="sectionHead"><div><h2>INFINITEPAY</h2><p>CONTA FINANCEIRA PRÓPRIA • D+1 ÚTIL</p></div><button onClick={sync}>SINCRONIZAR AGORA</button></div><div className="salesKpis"><div><small>SALDO DISPONÍVEL</small><b>{money(totals.saldo)}</b></div><div><small>VALORES A RECEBER</small><b>{money(totals.receber)}</b></div><div><small>BRUTO</small><b>{money(totals.bruto)}</b></div><div><small>TAXAS</small><b>{money(totals.taxa)}</b></div><div><small>LÍQUIDO</small><b>{money(totals.liquido)}</b></div></div><p className="note">Transferências InfinitePay ↔ Itaú são classificadas como transferência interna e não geram receita duplicada. Segredos permanecem somente no backend.</p><div className="salesTableWrap"><table className="salesTable"><thead><tr><th>DATA</th><th>VENDA/NSU</th><th>CLIENTE</th><th>BRUTO</th><th>TAXA</th><th>LÍQUIDO</th><th>PREVISÃO</th><th>STATUS</th></tr></thead><tbody>{tx.map(t=><tr key={t.id}><td>{t.data||"-"}</td><td>{t.numeroVenda||t.nsu||"-"}</td><td>{t.cliente||"-"}</td><td>{money(t.valorBruto)}</td><td>{money(t.taxa)}</td><td>{money(t.valorLiquido)}</td><td>{t.previsaoLiquidacao||"-"}</td><td>{t.status||"PENDENTE"}</td></tr>)}</tbody></table>{!tx.length&&<p className="muted">NENHUMA TRANSAÇÃO IMPORTADA. O PAINEL ESTÁ PRONTO PARA O CONECTOR HOMOLOGADO.</p>}</div></section>;
}
